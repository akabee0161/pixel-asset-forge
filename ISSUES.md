# 課題一覧

作業中に見つかって直していないものの置き場。**新しく見つけたらここに足すこと。**
報告して終わりにしない。直したら行ごと消す（経緯は git の履歴に残る）。

- **直す前に、まだ本当に課題かを確認すること。** このリストは書いた時点の状態である
- 将来は GitHub Issues に移す予定。リポジトリが発展途上で件数が多いうちは、ここで管理する
- GitHub Issues に既にあるものは「出どころ」に番号を書く

## 決めてもらう必要があるもの

勝手に決めないこと（README「確定していること / していないこと」）。

| 課題 | 詳細 | 出どころ |
|---|---|---|
| **タイルの受け渡し方が未定** | 出力は `render.py` の1枚ずつの PNG だけ。タイルセット（アトラス）で渡すのか、マップの完成品を渡すのか、ゲーム側で1枚ずつ組むのかが決まっていない。`tilemap.py` は検証用で、成果物ではない。README の未確定一覧には 2026-09-23 に追加済み | 2026-09-23 |
| **`knight` の瞳孔が輪郭に同化** | `outline` と `pupil` が同じ `#1a1228` で、`check_colors.py` が hard failure を出したまま。直すには `pupil` の色を決める必要があり、これは未確定項目。`hair_hi` と `cloth_hi` にも同じ重複があるが、この2色は現時点でどのアセットでも隣接していない | README（旧） |
| **`unit` で許す最小の線幅** | ゲームは canvas を 0.796倍で描くので、1〜2px の刃は丸ごと落ちる。振りかぶりコマの剣が消えた。刃を2px以上にするか、細い線に頼らない構図にする。**`unit` を増やす前に決めたい**（18枚に波及） | README（旧） |
| **`unit` が1体しか無い** | 残り8体（味方3・敵5）。`garum` は `frame=48` なので規約の再検討が要る。絵柄が混ざるので、増やすなら一気に揃えたい | README（旧） |

## ツールの不具合

| 課題 | 詳細・直し方の見当 | 大きさ | 出どころ |
|---|---|---|---|
| **Python 3.10 でテストが3件落ちる** | `tests/test_tools.py` の `TargetPathTest` が 3.11 以降にしかない `contextlib.chdir` を使っている。対応する Python のバージョンもどこにも書かれていない。`os.chdir` を try/finally で戻す小さなヘルパーに置き換えるか、3.11 以上を README に明記する | 小 | 2026-09-23 |
| **`check_colors.py` が既定の実行で毎回失敗する** | 原因は上の `knight` 1件だけだが、そのせいで新しい hard failure が出ても終了コードで区別できない。`knight` の色が決まれば解消する | 小（色の決定待ち） | 2026-09-23 |
| **`sheet.py` が列数を検査しない** | 12行は強制するが列数は見ていない。3列の定義を書くと 96×384 が黙って出て、気付くのは character-tactics の `sheet-size.test.ts` まで遅れる。ただし列数はユニットごとに変わりうる（`garum` は `frame=48`）ので、4列固定の検査は誤り | 小 | README（旧） |

## ツールの改善（挙動は変えない）

| 課題 | 詳細・直し方の見当 | 大きさ | 出どころ |
|---|---|---|---|
| **同じ処理が4本のツールに重複** | 「パレット読み込み → 対象収集 → 読み込み → 検査 → FAIL 表示」が `validate` / `render` / `check_colors` / `contact_sheet` にある。「読み込み＋検査＋画像化」も `tilemap.load_tile` と `sheet.load_frame` に重複 | 中 | 2026-09-23 |
| **ツール同士が互いの本体から関数を借りている** | `render` / `check_colors` / `contact_sheet` が `collect` / `default_targets` を `validate.py` から、`sheet.py` が `read_layout` を `tilemap.py` から import している。共通部分は `gridfile.py` に置くのが自然 | 小 | 2026-09-23 |
| **共有関数が `SystemExit` を投げる** | `tilemap.read_layout` は `sheet.py` からも呼ばれるのに、エラー時に `SystemExit` で終了する。`load_tile` は型注釈が `str` なのに `Path` も受け付けている | 小 | 2026-09-23 |
| **パレットを差し替えられない** | `palette/master.json` のパスが `tools/gridfile.py` で固定で、別のパレットを渡すオプションがどのツールにも無い。2Pカラーのような色違いを作るなら `--palette` が要る。必要になるまで作らない | 小 | 2026-09-23 |

## アセット・構成

| 課題 | 詳細・直し方の見当 | 大きさ | 出どころ |
|---|---|---|---|
| **レイアウトに置かれていないタイルが11点** | `bridge_v` `dirt` `fence_v` `mountain_{ne,nw,se,sw}` `path_sw` `path_t_{n,s,w}`。CLAUDE.md の「全点を置いたレイアウトを `layouts/` に足す」を満たしていない。特に `mountain_*` の4点は、どう組む想定かがマップで確認されていない | 小 | 2026-09-23 |
| **`item` の `SPEC.md` と、`tile` / `item` の `reference/` が無い** | `tile` と `unit` の `SPEC.md` は 2026-09-23 に README から規約を移して作った。`item` は `types/item/` 自体が無い（1点しか無いので保留でよい）。`tile` は `reference/` が無く、当面 `layouts/` のマップをお手本にしている | 小 | GitHub #1 |
| **`unit` の reference が本体のコピー** | `types/unit/reference/roran_{down,up,left}_base.txt` は `assets/unit/roran/*_base.txt` と行が完全に同じ。片方を直すともう片方とずれる。`contact_sheet.py` でも unit が28点（24＋4）と重複して並ぶ | 小 | 2026-09-23 |
| **`seed/face32.py` が実行経路に無い** | リポジトリ化前の原型で、分解済み。git の履歴に残るので削除できる | 小 | 2026-09-23 |

## 絵

| 課題 | 直し方の見当 | 大きさ | 出どころ |
|---|---|---|---|
| **山の陰影が宣言と食い違う** | `mountain` と `mountain_{nw,ne,sw,se}` は5枚とも明るい面が右下側にあり、`# light: upper-left` の宣言と逆。5枚どうしは揃っているので並べても破綻はしないが、他のタイルとは光の向きが違う（`layouts/field.txt` と contact sheet で目に見える）。単峰を作り直したときに2x2の側へ合わせたので、直すなら5枚まとめて | 5枚の描き直し | README（旧） |
| **城(2x2)の周りだけ草が平ら** | `castle_{nw,ne,sw,se}` の草の余白に `grass_shadow` の房を2〜3個ずつ足す | 数ピクセル×4枚 | README（旧） |
| **道と畑の土が同系色** | `path_*` と `crop_field` はどちらも `dirt_*` なので、畑を横切る道の境界が弱い。畑の土を一段暗くするか、道に縁を入れる | 小 | README（旧） |
| **森・崖の縁タイルが無い** | `forest` を敷くと境界が直角になる。縁を作るなら4〜8点、崖（高低差）はさらに別サブシステムで、どちらも単独の依頼として設計したい | 大 | README（旧） |
| **`stump` がまだ樽か木箱に見える（主観）** | README では「木箱に見える」を直した後の版だが、`layouts/field.txt` に置いた状態で見て、2026-09-23 時点でもその印象が残った。1人の目視なので、別の目でも確認したい | 小 | 2026-09-23 |
| **`layouts/field.txt` の川が途切れている** | 右端の `river_v` は3行目で草地にぶつかって終わり、U字の内側の `river_v` は `river_h` に合流せず岸で閉じる。タイルではなくレイアウトの組み方の問題。検証用マップとしては、繋がっていない箇所があると継ぎ目の確認が弱くなる | 小 | 2026-09-23 |

## `unit` とゲーム側

| 課題 | 詳細・直し方の見当 | 大きさ | 出どころ |
|---|---|---|---|
| **`unit` の足元アンカーが無い** | character-tactics の `drawMapUnit` は `center - frame/2` で貼るので論理位置がキャラの腰に来る。HPバー・リング・旗が腰基準になり、将来16pxタイルを敷くとほぼ1タイル下に立って見える。`src/render/sprites.ts` の1行 | 小（ゲーム側） | README（旧） |
| **`unit` の `atk_hit` がゲーム内で未確認** | 約300コマ連写しても打ち込みの姿勢を捉えられなかった。0.25秒のうち約83ミリ秒しか出ず、縮小で刃が落ちるため。確実に捉えるには `battle.time` を止める手段か、コマ番号を画面に出すデバッグ表示が要る | 小（確認手段の問題） | README（旧） |

## 文書

| 課題 | 詳細・直し方の見当 | 大きさ | 出どころ |
|---|---|---|---|
| **README の手順が実行で確かめられていない部分がある** | 2026-09-23 に人間向けの手引きへ書き直した。2章のツアーは実行して確かめたが、4章（Claude への依頼手順）は書いただけで、その手順で新しいアセットを作ったことはまだ無い。最初の依頼で手順どおりに進むか確かめ、食い違えば直す | 小 | 2026-09-23 |

## 「並べないと見えない」欠陥に機械チェックが無い

tile 型の欠陥は3種類とも、**1枚の `_x8.png` を見ても分からず、並べて初めて見える**。
いずれも `validate.py` と `check_colors.py` を通過する。

| 種類 | 見つかった経緯 | 機械チェックの状況 |
|---|---|---|
| 継ぎ目が繋がらない | 川の初版（`tilemap.py` を作る動機になった） | **実測して不採用。** トーラス巡回行差分では正常な城・曲がりと分離できなかった |
| 敷き詰めると反復が格子に見える | `plain` を3案並べ比べて判明 | **無い。案も無い** |
| マップに置くと別の物に見える | 切り株・井戸・道標 | **無い。** 「何に見えるか」なので、そもそも機械の領分か疑わしい |

現状これらは全部、目視で `tilemap.py --repeat` と `--layout` を見ることで拾っている。
**手順としては回っているが、見落としたときに止める仕組みが無い**のが課題である。

新しいチェックを思いつく前に、
[「深いチェックを試して2回落としている」](docs/2026-09-23-findings.md#深いチェックを試して2回落としている)を読むこと。
機械チェックの案は3回とも、実装して既存アセット全件に掛けた時点で撤回か下方修正に
なっている。**まず偽陽性を数えること。**
